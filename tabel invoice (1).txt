create table invoice
(invoiceno integer,
indate date,
item varchar(20),
price float,
qty float,
total float,
mode varchar(20),
custname varchar(30),
address varchar(150),
phone varchar(20));

#use this query to create a table in mysql database.
